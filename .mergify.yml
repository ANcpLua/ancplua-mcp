# Mergify Configuration
# Safe auto-merge rules for ancplua-mcp repository
# Documentation: https://docs.mergify.com

queue_rules:
  - name: default
    merge_method: merge
    commit_message_template: |
      {{ title }} (#{{ number }})

      {{ body }}
    merge_conditions:
      # All CI checks must pass
      - check-success=Build and Test
      - check-success=Code Quality
      - check-success=Analyze (csharp)
      - check-success=Security Scan

      # Require at least 1 approval
      - "#approved-reviews-by>=1"

      # No changes requested
      - "#changes-requested-reviews-by=0"

      # Branch must be up to date
      - base=main
      - -draft

  - name: renovate
    merge_method: squash
    commit_message_template: |
      {{ title }} (#{{ number }})
    merge_conditions:
      - check-success=Build and Test
      - check-success=Code Quality
      - base=main

  - name: dependabot
    merge_method: squash
    commit_message_template: |
      {{ title }} (#{{ number }})
    merge_conditions:
      - check-success=Build and Test
      - check-success=Code Quality
      - base=main

pull_request_rules:
  # Auto-merge for Renovate dependency updates (minor/patch only)
  - name: Auto-merge Renovate minor/patch updates
    conditions:
      - author=renovate[bot]
      - check-success=Build and Test
      - check-success=Code Quality
      - -title~=(?i)major
      - -label~=breaking
    actions:
      queue:
        name: renovate

  # Auto-merge for Dependabot updates (minor/patch only)
  - name: Auto-merge Dependabot minor/patch updates
    conditions:
      - author=dependabot[bot]
      - check-success=Build and Test
      - check-success=Code Quality
      - -title~=(?i)major
      - -label~=breaking
    actions:
      queue:
        name: dependabot

  # Auto-merge approved PRs
  - name: Auto-merge approved PRs
    conditions:
      - "#approved-reviews-by>=1"
      - check-success=Build and Test
      - check-success=Code Quality
      - check-success=Analyze (csharp)
      - check-success=Security Scan
      - -draft
      - -label~=do-not-merge
      - -label~=work-in-progress
    actions:
      queue:
        name: default

  # Auto-label based on file changes
  - name: Label .NET changes
    conditions:
      - files~=\.cs$
    actions:
      label:
        add:
          - dotnet
          - csharp

  - name: Label project changes
    conditions:
      - files~=\.csproj$
    actions:
      label:
        add:
          - dotnet

  - name: Label workflow changes
    conditions:
      - files~=^\.github/workflows/
    actions:
      label:
        add:
          - ci-cd
          - github-actions

  - name: Label documentation changes
    conditions:
      - files~=\.md$
    actions:
      label:
        add:
          - documentation
